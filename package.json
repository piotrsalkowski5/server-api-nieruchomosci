{
  "name": "server-api-nieruchomosci",
  "version": "1.0.0",
  "private": true,
  "description": "Minimalny Node.js API template dla Vercel bez builda i bez app.listen",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "latest"
  }
}